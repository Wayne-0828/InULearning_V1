# =============================================================================
# InULearning 測試環境環境變數
# =============================================================================
# 此檔案用於測試環境，包含簡化的設定

# =============================================================================
# 資料庫密碼設定 (測試環境)
# =============================================================================

# PostgreSQL 資料庫設定
POSTGRES_DB=inulearning_test
POSTGRES_USER=aipe-tester
POSTGRES_PASSWORD=aipe-tester

# MongoDB 資料庫設定
MONGO_INITDB_ROOT_USERNAME=aipe-tester
MONGO_INITDB_ROOT_PASSWORD=aipe-tester
MONGO_INITDB_DATABASE=inulearning_test

# Redis 密碼設定
REDIS_PASSWORD=redis_password

# =============================================================================
# 對象儲存設定
# =============================================================================

# MinIO 存取金鑰
MINIO_ROOT_USER=minio_access_key
MINIO_ROOT_PASSWORD=minio_secret_key

# =============================================================================
# AI 服務 API 金鑰
# =============================================================================

# Gemini API 金鑰 (用於 AI 分析服務)
GEMINI_API_KEY=your_gemini_api_key_here

# =============================================================================
# 開發環境設定
# =============================================================================

# 環境變數
ENVIRONMENT=test

# 服務主機設定 (Docker 網路內)
POSTGRES_HOST=postgresql
MONGODB_HOST=mongodb
REDIS_HOST=redis
MILVUS_HOST=milvus
MINIO_HOST=minio 